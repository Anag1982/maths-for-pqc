# Lab 1 — Chapter 1, "What Quantum Computing Actually Breaks"
#
# Chapter 1 is deliberately the lightest lab in the book: no SageMath, no
# liboqs, no cryptographic library of any kind. That is part of the argument.
# From Chapter 4 onwards the labs need the full environment in Appendix B.
#
# Pinned so that a reader in 2029 gets the same output as the book shows.

numpy==2.3.4
matplotlib==3.10.7

# Only needed to re-execute the notebook headlessly (CI, or `make lab01`).
jupyter-core==5.9.1
nbformat==5.10.4
nbconvert==7.16.6
ipykernel==6.31.1
