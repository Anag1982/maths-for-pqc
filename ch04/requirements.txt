# Lab 4 — Chapter 4, "The Number-Theoretic Transform"
#
# ML-KEM's incomplete NTT (Algorithms 9-12 of FIPS 203) and both standards'
# Montgomery/Barrett reduction are written from scratch in plain Python --
# no NTT library, no SageMath, no sympy. Pinned so that a reader in 2029
# gets the same output as the book shows.

numpy==2.3.4
matplotlib==3.10.7

# Only needed to re-execute the notebook headlessly (CI, or `make lab04`).
jupyter-core==5.9.1
nbformat==5.10.4
nbconvert==7.16.6
ipykernel==6.31.1
